class Pair {
    constructor(tokenA, tokenB, reserveA, reserveB, exist) {
        this.tokenA = tokenA;
        this.tokenB = tokenB;
        this.reserveA = reserveA;
        this.reserveB = reserveB;
        this.exist = exist;
    }
}

export default Pair